import { FunctionComponent } from "react";
import Layout from "../layouts/Layout";

const Jobs: FunctionComponent = () => {
    return (
        <Layout>
            <div>Jobs</div>
        </Layout>
    );
};

export default Jobs;
