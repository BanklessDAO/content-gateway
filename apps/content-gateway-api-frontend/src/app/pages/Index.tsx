import { FunctionComponent } from "react";
import Layout from "../layouts/Layout";

const Index: FunctionComponent = () => {
    return (
        <Layout>
            <div>Welcome to Content Gateway!</div>
        </Layout>
    );
};

export default Index;
